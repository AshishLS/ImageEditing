# ImageEditing
Stage 1 - For the time being, this application matches size of two files of same name from different folder.
Stage 2 - Uses openCV to compare images. Takes care of the size of the images.
